apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: couple-games
spec:
  rules:
    - host: app.staging.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: couple-games
                port:
                  number: 8080
